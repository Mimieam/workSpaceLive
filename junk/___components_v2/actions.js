// client API actions - take a state and params - return a new state

export const create_window = (state) => { }
export const delete_window = (state, id) => { }
export const create_new_window = (state) => { }
export const create_new_window = (state) => { }
export const create_new_window = (state) => { }